[build]
  # Build from root context: target only landing-page workspace
  command = "chmod +x node_modules/.bin/* apps/landing-page/node_modules/.bin/* 2>/dev/null || true && npm run build --workspace=landing-page"
  publish = "apps/landing-page/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
